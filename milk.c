#include<stdio.h>
#define p printf
main()
{
    int b1=1,b2=5,b3=7,b4=10;
    int s=b1+b2+b3+b4;
    int t;
    p("Enter the quantity : ");
    /*
    scanf("%d",&t);
        if( t==b1 | t==b2 | t==b3 | t==b4 )
        {
            p(" 1 bottel ");
        }
    else if(t<b4)
    {
        if(b1+b2<=t | b2+b3<=t | b1+b3<=t)
            p(" 2 Bottel ");
        else
            p(" 3 Bottel ");
    }
    else if(t>b4)
    {
        if( (b1+b2+b3<=t | b1+b2+b4<=t | b2+b3+b4<=t | b3+b1+b4<=t ) & t<s)
            p(" 3 Bottel ");
        else
            p(" 4 Bottel ");
    }
    else
        p("more quantity");
        */

}
